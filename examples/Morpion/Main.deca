#include "Morpion.deca"

{

        Morpion morp=new Morpion();
        boolean fin=false;

        morp.jouer(1,1);
        morp.jouer(2,1);
        morp.finPartie();
        morp.jouer(3,1);
        morp.finPartie();

}
